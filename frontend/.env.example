# Base URL of the FastAPI backend.
# Vite only exposes env vars prefixed with VITE_ to browser code
# (see import.meta.env.VITE_API_BASE_URL in src/api/client.js).
VITE_API_BASE_URL=http://localhost:8000
